# Operations

Guia operacional inicial.

## Rotinas

- subir stack local com `make up`
- checar logs com `make logs`
- derrubar stack com `make down`

## Healthchecks

- `GET /health` em ambos serviços FastAPI
