[package]
name = "worker"
version = "0.1.0"
edition = "2021"
description = "OpenMesh-AI worker CLI"
license = "Apache-2.0"

[dependencies]
clap = { version = "4.5", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2.0"
